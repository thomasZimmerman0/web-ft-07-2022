collection = ['long', 'longer', 'longest']

def shortest(words):
    hold = words[0]
    for i in words:
        if len(i) < len(hold):
            hold = i
    return hold
            

print(shortest(collection))

