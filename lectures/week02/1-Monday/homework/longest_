collection = ['long', 'longer', 'longest']

def longest(words):
    hold = words[0]
    for i in words:
        if len(i) > len(hold):
            hold = i
    return hold
            

print(longest(collection))

